node_modules
dist
.output
.nuxt
.data

# Fixtures mirror the real Nuxt layout, where path mappings live in generated
# .nuxt configs, so these committed files must survive the ignores above.
!packages/cli/test/fixtures/**/.nuxt/
!packages/cli/test/fixtures/**/.nuxt/**
coverage
*.log
.DS_Store
.env
.env.*
!.env.example
tmp
*.tsbuildinfo
*.tgz

# One-shot local bootstrap that publishes the 0.0.0 placeholder with a personal
# npm login. It exists so npm has a package to attach a trusted publisher to;
# every real release is cut by release-please through OIDC.
scripts/bootstrap-npm-publish.sh
